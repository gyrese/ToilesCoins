import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.monnaievirtuelle.app',
  appName: 'Monnaie Virtuelle',
  webDir: 'out'
};

export default config;
